import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.naltylabs.talkive', // PASTIKAN KECIL SEMUA DI SINI
  appName: 'Talkive',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;